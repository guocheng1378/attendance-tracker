let currentDate=new Date(),selectedStatuses={},currentLang=localStorage.getItem('att_lang')||CONFIG.DEFAULT_LANG;
...
async function loadSaved(){const dk=formatDate(currentDate);try{const r=await fetch(CONFIG.JSONBIN_URL+'/b/'+CONFIG.JSONBIN_BIN_ID+'/latest',{headers:{'X-Master-Key':CONFIG.JSONBIN_API_KEY}});const d=await r.json();const rows=d.record||[];selectedStatuses={};rows.forEach(x=>{if(x.date===dk&&x.worker_id&&x.status)selectedStatuses[x.worker_id]=x.status;});}catch(e){selectedStatuses={};}
...