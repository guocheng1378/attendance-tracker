let currentDate=new Date(),selectedStatuses={},currentLang=localStorage.getItem('att_lang')||CONFIG.DEFAULT_LANG;
let syncToken=localStorage.getItem('att_token')||'';
let syncData=[];

function gistRead(){
    if(!syncToken||!CONFIG.GIST_ID){syncData=[];try{syncData=JSON.parse(localStorage.getItem('att_data'));}catch(e){}return Promise.resolve();}
    return fetch('https://api.github.com/gists/'+CONFIG.GIST_ID,{headers:{'Authorization':'token '+syncToken,'Accept':'application/vnd.github.v3+json'}}).then(function(r){return r.json();}).then(function(d){if(d.files&&d.files[CONFIG.GIST_FILE]&&d.files[CONFIG.GIST_FILE].content){syncData=JSON.parse(d.files[CONFIG.GIST_FILE].content);}else{syncData=[];}}).catch(function(e){syncData=[];try{syncData=JSON.parse(localStorage.getItem('att_data'));}catch(ex){}});
}

function gistWrite(){
    localStorage.setItem('att_data',JSON.stringify(syncData));
    if(!syncToken||!CONFIG.GIST_ID)return Promise.resolve();
    return fetch('https://api.github.com/gists/'+CONFIG.GIST_ID,{method:'PATCH',headers:{'Authorization':'token '+syncToken,'Content-Type':'application/json','Accept':'application/vnd.github.v3+json'},body:JSON.stringify({files:{[CONFIG.GIST_FILE]:{content:JSON.stringify(syncData)}}})}).catch(function(e){});
}

document.addEventListener('DOMContentLoaded',function(){currentDate=new Date();setLang(currentLang);renderDate();renderWorkerList();});
function setLang(lang){currentLang=lang;localStorage.setItem('att_lang',lang);var T=lang==='lo'?CONFIG.LO:CONFIG.ZH;document.querySelectorAll('.lang-btn').forEach(function(b){b.classList.toggle('active',b.dataset.lang===lang);});document.getElementById('page-title').textContent=T.TITLE;document.getElementById('page-sub').textContent=T.SUBTITLE;document.getElementById('s-full-label').textContent=T.FULL;document.getElementById('s-half-label').textContent=T.HALF;document.getElementById('s-absent-label').textContent=T.ABSENT;document.getElementById('save-label').textContent=T.SAVE;document.getElementById('admin-link-text').textContent=T.ADMIN;renderWorkerList();}
function formatDate(d){return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0');}
function renderDate(){document.getElementById('current-date').textContent=currentDate.toLocaleDateString(currentLang==='lo'?'lo-LA':'zh-CN',{year:'numeric',month:'long',day:'numeric'});document.getElementById('current-day').textContent=currentDate.toLocaleDateString(currentLang==='lo'?'lo-LA':'zh-CN',{weekday:'long'});}
function changeDate(delta){currentDate.setDate(currentDate.getDate()+delta);renderDate();loadSaved();updateSummary();}
function renderWorkerList(){var T=currentLang==='lo'?CONFIG.LO:CONFIG.ZH;var c=document.getElementById('worker-list');c.innerHTML='';CONFIG.EMPLOYEES.forEach(function(emp){var card=document.createElement('div');card.className='worker-card';card.innerHTML='<div class="worker-name">'+emp.name+'<span class="worker-zh">'+emp.nameZh+'</span></div><div class="status-btns"><button class="status-btn full" onclick="selectStatus('+emp.id+',\'full\',this)"><span class="s-icon">✅</span><span>'+T.FULL+'</span></button><button class="status-btn half" onclick="selectStatus('+emp.id+',\'half\',this)"><span class="s-icon">⚠️</span><span>'+T.HALF+'</span></button><button class="status-btn absent" onclick="selectStatus('+emp.id+',\'absent\',this)"><span class="s-icon">❌</span><span>'+T.ABSENT+'</span></button></div>';c.appendChild(card);});loadSaved();updateSummary();}
function selectStatus(id,status,btn){selectedStatuses[id]=status;var card=btn.closest('.status-btns');card.querySelectorAll('.status-btn').forEach(function(b){b.classList.remove('selected');});btn.classList.add('selected');updateSummary();}
function updateSummary(){var full=0,half=0,absent=0;Object.values(selectedStatuses).forEach(function(s){if(s==='full')full++;else if(s==='half')half++;else if(s==='absent')absent++;});document.getElementById('s-full').textContent=full;document.getElementById('s-half').textContent=half;document.getElementById('s-absent').textContent=absent;}
function loadSaved(){var dk=formatDate(currentDate);gistRead().then(function(){selectedStatuses={};syncData.forEach(function(x){if(x.date===dk&&x.worker_id&&x.status)selectedStatuses[x.worker_id]=x.status;});document.querySelectorAll('.worker-card').forEach(function(card,i){var empId=CONFIG.EMPLOYEES[i].id;var st=selectedStatuses[empId];card.querySelectorAll('.status-btn').forEach(function(b){b.classList.remove('selected');});if(st)card.querySelector('.status-btn.'+st).classList.add('selected');});updateSummary();});}
function saveRecord(){var dk=formatDate(currentDate);if(Object.keys(selectedStatuses).length===0){alert(currentLang==='lo'?'ກະລຸນາເລືອກສະຖານະກ່ອນບັນທຶກ':'请先选择考勤状态');return;}var T=currentLang==='lo'?CONFIG.LO:CONFIG.ZH;var btn=document.getElementById('save-btn');btn.textContent='...';btn.disabled=true;gistRead().then(function(){syncData=syncData.filter(function(x){return x.date!==dk;});CONFIG.EMPLOYEES.forEach(function(emp){syncData.push({date:dk,worker_id:emp.id,name:emp.name,name_zh:emp.nameZh,status:selectedStatuses[emp.id]||'absent',saved_at:new Date().toISOString()});});return gistWrite();}).then(function(){btn.textContent=T.SAVED;btn.classList.add('saved');btn.disabled=false;setTimeout(function(){btn.textContent=T.SAVE;btn.classList.remove('saved');},2000);}).catch(function(){btn.textContent=T.SAVE;btn.disabled=false;alert(currentLang==='lo'?'ເກີດຂໍ້ຜິດພາດ':'保存失败');});}
function showTokenModal(){var T=currentLang==='lo'?CONFIG.LO:CONFIG.ZH;var overlay=document.createElement('div');overlay.style.cssText='position:fixed;top:0;left:0;right:0;bottom:0;background:rgba(0,0,0,0.5);z-index:100;display:flex;align-items:center;justify-content:center;';var box=document.createElement('div');box.style.cssText='background:white;border-radius:16px;padding:24px;width:90%;max-width:360px;text-align:center;';box.innerHTML='<h3 style="margin:0 0 8px">'+T.SET_TOKEN+'</h3><p style="color:#888;font-size:13px;margin:0 0 16px">GitHub Personal Access Token</p><input id="token-input" type="text" placeholder="ghp_..." style="width:100%;padding:12px;border:1px solid #ddd;border-radius:8px;font-size:14px;box-sizing:border-box" value="'+syncToken+'"><p id="token-msg" style="color:#888;font-size:12px;margin:8px 0 0;min-height:18px"></p><div style="display:flex;gap:10px;margin-top:16px"><button id="token-cancel" style="flex:1;padding:10px;border:1px solid #ddd;border-radius:8px;background:white;cursor:pointer">✕</button><button id="token-save" style="flex:1;padding:10px;border:none;border-radius:8px;background:#28a745;color:white;cursor:pointer">OK</button></div>';overlay.appendChild(box);document.body.appendChild(overlay);var inp=document.getElementById('token-input');var msg=document.getElementById('token-msg');document.getElementById('token-cancel').onclick=function(){overlay.remove();};document.getElementById('token-save').onclick=function(){var v=inp.value.trim();if(!v){inp.style.borderColor='red';msg.textContent=currentLang==='lo'?'ກະລຸນາໃສ່ Token':'请输入Token';msg.style.color='red';return;}setToken(v);msg.textContent='✅ '+(currentLang==='lo'?'Token ບັນທຶກແລ້ວ':'Token 已保存');msg.style.color='#22c55e';setTimeout(function(){overlay.remove();},1000);};}
function setToken(t){syncToken=t;localStorage.setItem('att_token',t);}
